import { JourneyExperience } from "../components/journey-experience";

export default function Home() {
  // A1 replaces this with the real city + decade form.
  return <JourneyExperience isWorld={false} />;
}
