// Normalized control vocabulary emitted by the UI.
// Each adapter maps these to its own SDK calls — per-model mapping tables land in A3.

export type MoveDir = "forward" | "back" | "left" | "right" | "fl" | "fr" | "bl" | "br";

export type LookDir = "left" | "right" | "up" | "down";
